#include<iostream>
#include<string>
using namespace std;
int main() {
	int a, b;
	while (1) {
		cin >> a >> b;
		if (cin.eof())break;
		cout << a + b << endl;		
	}
	return 0;
}
/*
EOF 는 end of file의 약자, 윈도우로는 컨트롤z, 맥에서는 컨트롤 d, 파일의 끝 또는 종료를 의미한다.
cin.eof()는 입력값이 eof이면 true, 아니면 false를 반환한다.
*/
